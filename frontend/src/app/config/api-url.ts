import { environment } from '../../environments/environment';

/**
 * URL base de la API (incluye el prefijo `/api`).
 * Importa este símbolo donde necesites el endpoint global.
 */
export const API_URL = environment.apiUrl;
